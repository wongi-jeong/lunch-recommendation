# 이 파일을 .env 로 복사한 뒤 값을 채우세요:  cp .env.example .env
# .env 는 커밋되지 않습니다.

# Google Maps JavaScript API 키 (지도 표시용).
# ⚠️ 클라이언트(브라우저)에 노출되는 키입니다. Google Cloud에서 반드시
#    "HTTP 리퍼러 제한"을 걸고, 서버측(GOOGLE_MAPS_API_KEY)과 별도 키를 사용하세요.
VITE_GOOGLE_MAPS_API_KEY=your_client_side_google_maps_js_key
